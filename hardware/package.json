{
  "name": "agripod-pod-bridge",
  "private": true,
  "type": "module",
  "description": "Serial-to-backend bridge for the AgriPod hardware pod (USB phase)",
  "scripts": {
    "bridge": "node pod-bridge.mjs"
  },
  "dependencies": {
    "@serialport/parser-readline": "^12.0.0",
    "serialport": "^12.0.0"
  }
}
